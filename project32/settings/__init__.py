"""
Settings package for Project32.

Use one of:
- project32.settings.dev
- project32.settings.prod

`manage.py` defaults to `dev`, while `wsgi.py` defaults to `prod`.
You can override by setting DJANGO_SETTINGS_MODULE in the environment.
"""
